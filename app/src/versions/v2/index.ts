import type { Version } from '../types';
import { scenes } from './scenes';

/**
 * Iteration 2 — the "M&A event" story. An acquisition turns two Lloyds clients
 * into one group; Daisy originates, shapes and progresses the group mandate to
 * a win across eight scenes, with a persistent deal thread and a unified
 * platform close.
 */
export const v2: Version = {
  id: 'v2',
  label: 'Iteration 2',
  blurb: 'M&A event · two clients become one group',
  scenes,
};
