import type { Version } from '../types';
import { scenes } from './scenes';

/**
 * Iteration 2 — the "RM wins the deal" reframe. Daisy originates, shapes and
 * progresses one structured Avonmore deal through a journey of six scenes,
 * with a persistent deal thread running across the desktop cockpit.
 */
export const v2: Version = {
  id: 'v2',
  label: 'Iteration 2',
  blurb: 'Reframe · the RM wins the deal',
  scenes,
};
